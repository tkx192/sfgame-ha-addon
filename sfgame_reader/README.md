# Shakes & Fidget Reader Home Assistant App

Read-only monitoring of a Shakes & Fidget character in Home Assistant.

The app deliberately uses the upstream `sf-api` implementation for login, session handling, encryption/decryption, response parsing, and GameState modeling instead of reimplementing the game protocol.
